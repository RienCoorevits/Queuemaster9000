{
  "name": "@queuemaster/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "dev": "wait-on tcp:5173 && electron .",
    "start": "electron .",
    "build": "node -e \"console.log('Desktop shell build uses the dashboard bundle from apps/dashboard/dist.')\"",
    "typecheck": "node -e \"process.exit(0)\""
  },
  "devDependencies": {
    "electron": "^33.3.1"
  }
}

